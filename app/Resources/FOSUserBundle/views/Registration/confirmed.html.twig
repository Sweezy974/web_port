{% extends "@FOSUser/layout.html.twig" %}

{% trans_default_domain 'FOSUserBundle' %}

{% block body %}

<div class="row">
    <div class="col s12 center">
        <a href="{{ path('profile_new')}}" class="btn ">modifier mon profil</a>
    </div>
</div>

{% endblock %}
